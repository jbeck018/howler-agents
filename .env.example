# =============================================================================
# Howler Agents - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
# LiteLLM auto-reads standard provider env vars (ANTHROPIC_API_KEY, etc.)

# --- LLM Provider Keys (BYOK - Bring Your Own Keys) ---
# Set the key(s) for whichever provider(s) you want to use.
# LiteLLM routes based on model name prefix (e.g., "claude-*" -> Anthropic).
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# Optional: for other providers (Ollama, Azure, etc.)
# OLLAMA_API_BASE=http://localhost:11434
# AZURE_API_KEY=
# AZURE_API_BASE=

# --- LLM Role Mapping ---
# Which model to use for each evolutionary role.
# Format: provider/model or just model name (LiteLLM resolves).
HOWLER_LLM_ACTING_MODEL=claude-sonnet-4-20250514
HOWLER_LLM_EVOLVING_MODEL=claude-sonnet-4-20250514
HOWLER_LLM_REFLECTING_MODEL=claude-sonnet-4-20250514

# --- Database (PostgreSQL + pgvector) ---
DATABASE_URL=postgresql+asyncpg://howler:howler@localhost:5432/howler_agents
# For migrations (sync driver)
DATABASE_URL_SYNC=postgresql://howler:howler@localhost:5432/howler_agents

# --- Redis ---
REDIS_URL=redis://localhost:6379/0

# --- Service Ports ---
GRPC_PORT=50051
REST_PORT=8080
UI_PORT=3000

# --- Evolution Defaults ---
HOWLER_POPULATION_SIZE=10
HOWLER_GROUP_SIZE=3
HOWLER_NUM_ITERATIONS=5
HOWLER_ALPHA=0.5
HOWLER_NUM_PROBES=20

# --- Authentication ---
JWT_SECRET=change-me-to-a-random-secret
# ACCESS_TOKEN_EXPIRE_MINUTES=60
# REFRESH_TOKEN_EXPIRE_DAYS=30

# --- CORS ---
CORS_ORIGINS=http://localhost:3000

# --- Logging ---
LOG_LEVEL=INFO
LOG_FORMAT=json
